# AANA Calendar Scheduling Guardrail Skill

Marketplace slug:

```text
aana-calendar-scheduling-guardrail
```

Checks attendees, timezone, date, duration, recurrence, agenda, private notes, and approval before calendar changes.

This package is instruction-only. It does not execute code, call calendar services, write files, persist memory, or install dependencies.
