# AANA Ticket Update Checker Skill

Marketplace slug:

```text
aana-ticket-update-checker
```

Checks support, issue, CRM, and task updates for evidence, visibility, owner/status changes, private data, and approval.

This package is instruction-only. It does not execute code, call ticket systems, write files, persist memory, or install dependencies.
