# AANA Data Export Guardrail Skill

Marketplace slug:

```text
aana-data-export-guardrail
```

Checks export scope, destination, privacy, redaction, and approval before files, records, datasets, logs, or account data are exported or shared.

This package is instruction-only. It does not run programs, export data, call services, write files, persist memory, or install dependencies.
