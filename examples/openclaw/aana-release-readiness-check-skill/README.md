# AANA Release Readiness Check Skill

Marketplace slug:

```text
aana-release-readiness-check
```

Checks release targets, tags, changelogs, docs, artifacts, tests, approval, and rollback readiness before a release.

This package is instruction-only. It does not execute code, create tags, publish releases, write files, persist memory, or install dependencies.
